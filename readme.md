This is a simple RestAPI built with just two fields, "name" and "alias" using python3, django and djangorestframework.

You can run the server with python3 manage.py runserver and follow the instructions. 
